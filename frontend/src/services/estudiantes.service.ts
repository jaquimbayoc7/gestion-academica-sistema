// TODO: HU-01 — implementar servicios de Estudiantes
// import { apiGet, apiPost, apiPut, apiDelete } from '@/lib/api';
// import type { Estudiante } from '@/interfaces/estudiante.interface';

export const estudiantesService = {
  // findAll: () => apiGet<Estudiante[]>('/estudiantes'),
  // findOne: (id: number) => apiGet<Estudiante>(`/estudiantes/${id}`),
  // create: (data: unknown) => apiPost<Estudiante>('/estudiantes', data),
  // update: (id: number, data: unknown) => apiPut<Estudiante>(`/estudiantes/${id}`, data),
  // remove: (id: number) => apiDelete<void>(`/estudiantes/${id}`),
};
